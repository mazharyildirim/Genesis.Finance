@using Genesis.Shared
@using Genesis.WebApp.Models
@if (Messages!=null && Messages.detail != null)
{
    <p class="red">@Messages.detail</p>
}


@code {
    [Parameter]
    public ErrorMessages Messages { get; set; } = null;
}
